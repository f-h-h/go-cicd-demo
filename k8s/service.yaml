apiVersion: v1
kind: Service
metadata:
  name: go-cicd-demo
  namespace: default
spec:
  selector:
    app: go-cicd-demo
  ports:
    - port: 80
      targetPort: 8080
  type: LoadBalancer  # Killercoda 会分配一个外部 IP（实际上可能是 NodePort）